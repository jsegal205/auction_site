<p style="color: green"><%= notice %></p>

<% content_for :title, "Bids" %>

<h1>Bids</h1>

<div id="bids">
  <% @bids.each do |bid| %>
    <%= render bid %>
    <p>
      <%= link_to "Show this bid", bid %>
    </p>
  <% end %>
</div>

<%= link_to "New bid", new_bid_path %>
